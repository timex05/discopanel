syntax = "proto3";

package discopanel.v1;

import "discopanel/options/v1/options.proto";

option go_package = "github.com/discohaus/discopanel/pkg/proto/discopanel/v1;discopanelv1";

// Server file management
service FileService {
  // Browse server directory
  rpc ListFiles(ListFilesRequest) returns (ListFilesResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_READ
      object_id_field: "server_id"
    };
  }
  // Download file content
  rpc GetFile(GetFileRequest) returns (GetFileResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_READ
      object_id_field: "server_id"
    };
  }
  // Save an uploaded file
  rpc SaveUploadedFile(SaveUploadedFileRequest) returns (SaveUploadedFileResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_CREATE
      object_id_field: "server_id"
    };
  }
  // Modify file content
  rpc UpdateFile(UpdateFileRequest) returns (UpdateFileResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_UPDATE
      object_id_field: "server_id"
    };
  }
  // Remove file or directory
  rpc DeleteFile(DeleteFileRequest) returns (DeleteFileResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_DELETE
      object_id_field: "server_id"
    };
  }
  // Change file name
  rpc RenameFile(RenameFileRequest) returns (RenameFileResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_UPDATE
      object_id_field: "server_id"
    };
  }
  // Unzip archive file
  rpc ExtractArchive(ExtractArchiveRequest) returns (ExtractArchiveResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_UPDATE
      object_id_field: "server_id"
    };
  }
  // Create a new folder
  rpc CreateFolder(CreateFolderRequest) returns (CreateFolderResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_CREATE
      object_id_field: "server_id"
    };
  }
  // Move a file or directory
  rpc MoveFile(MoveFileRequest) returns (MoveFileResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_UPDATE
      object_id_field: "server_id"
    };
  }
  // Copy a file or directory
  rpc CopyFile(CopyFileRequest) returns (CopyFileResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_CREATE
      object_id_field: "server_id"
    };
  }
  // Create a zip archive from selected paths
  rpc CreateArchive(CreateArchiveRequest) returns (CreateArchiveResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_CREATE
      object_id_field: "server_id"
    };
  }
  // Download multiple files/folders as a zip
  rpc DownloadArchive(DownloadArchiveRequest) returns (DownloadArchiveResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_READ
      object_id_field: "server_id"
    };
  }
  // Starts a download session, bytes ride GET download/{session_id}
  rpc InitFileDownload(InitFileDownloadRequest) returns (InitFileDownloadResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_READ
      object_id_field: "server_id"
    };
  }
  // Poll extraction progress
  rpc GetExtractionStatus(GetExtractionStatusRequest) returns (GetExtractionStatusResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_READ
      object_id_field: "server_id"
    };
  }
  // Browse the panel host filesystem one directory at a time
  rpc ListHostFiles(ListHostFilesRequest) returns (ListHostFilesResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_SETTINGS
      action: ACTION_TYPE_UPDATE
    };
  }
  // Browse inside a running container one directory at a time
  rpc ListContainerFiles(ListContainerFilesRequest) returns (ListContainerFilesResponse) {
    option (discopanel.options.v1.perm) = {
      resource: RESOURCE_TYPE_FILES
      action: ACTION_TYPE_READ
      object_id_field: "server_id"
    };
  }
}

// File metadata and tree
message FileInfo {
  string name = 1;
  string path = 2;
  bool is_dir = 3;
  int64 size = 4;
  int64 modified = 5;
  bool is_editable = 6;
  repeated FileInfo children = 7;
}

// Directory listing parameters
message ListFilesRequest {
  string server_id = 1;
  string path = 2;
  bool tree = 3;
}

// Directory contents
message ListFilesResponse {
  repeated FileInfo files = 1;
}

// File download parameters
message GetFileRequest {
  string server_id = 1;
  string path = 2;
}

// File binary content
message GetFileResponse {
  bytes content = 1;
  string mime_type = 2;
}

// File upload data
message SaveUploadedFileRequest {
  string server_id = 1;
  string upload_session_id = 2;
  string destination_path = 3; // Target dir
  string filename = 4; // Target filename
}

// Save uploaded file result
message SaveUploadedFileResponse {
  string message = 1;
  string path = 2;
}

// File content update
message UpdateFileRequest {
  string server_id = 1;
  string path = 2;
  bytes content = 3;
}

// Update confirmation
message UpdateFileResponse {
  string message = 1;
  string path = 2;
}

// File to delete
message DeleteFileRequest {
  string server_id = 1;
  string path = 2;
  repeated string paths = 3; // Bulk delete
}

// Empty delete response
message DeleteFileResponse {}

// Folder creation parameters
message CreateFolderRequest {
  string server_id = 1;
  string path = 2;
}

// Folder creation result
message CreateFolderResponse {
  string message = 1;
}

// File move parameters
message MoveFileRequest {
  string server_id = 1;
  string source_path = 2;
  string destination_path = 3;
}

// File move result
message MoveFileResponse {
  string message = 1;
}

// File copy parameters
message CopyFileRequest {
  string server_id = 1;
  string source_path = 2;
  string destination_path = 3;
}

// File copy result
message CopyFileResponse {
  string message = 1;
}

// Archive creation parameters
message CreateArchiveRequest {
  string server_id = 1;
  repeated string paths = 2;
  string destination_path = 3;
  string archive_name = 4;
}

// Archive creation result
message CreateArchiveResponse {
  string message = 1;
  string archive_path = 2;
  int32 files_archived = 3;
}

// Archive download parameters
message DownloadArchiveRequest {
  string server_id = 1;
  repeated string paths = 2;
}

// Archive download session
message DownloadArchiveResponse {
  string session_id = 1;
  string filename = 2;
  int64 total_size = 3;
}

// File rename parameters
message RenameFileRequest {
  string server_id = 1;
  string path = 2;
  string new_name = 3;
}

// Rename result
message RenameFileResponse {
  string message = 1;
  string new_path = 2;
}

// Archive to extract
message ExtractArchiveRequest {
  string server_id = 1;
  string path = 2;
}

// Extraction started
message ExtractArchiveResponse {
  string operation_id = 1;
}

// Extraction status poll
message GetExtractionStatusRequest {
  string operation_id = 1;
  string server_id = 2;
}

// Archive extraction lifecycle
enum ExtractionState {
  EXTRACTION_STATE_UNSPECIFIED = 0;
  EXTRACTION_STATE_EXTRACTING = 1;
  EXTRACTION_STATE_COMPLETED = 2;
  EXTRACTION_STATE_FAILED = 3;
}

// Extraction progress
message GetExtractionStatusResponse {
  ExtractionState state = 1;
  int32 files_extracted = 2;
  string error = 3;
}

// Single file download session
message InitFileDownloadRequest {
  string server_id = 1;
  string path = 2;
}

// Host directory to list
message ListHostFilesRequest {
  string path = 1; // Directory to list, empty means the filesystem root
}

// Host directory contents, one level deep
message ListHostFilesResponse {
  string path = 1; // Absolute directory that was listed
  repeated FileInfo files = 2;
}

// Container directory to list
message ListContainerFilesRequest {
  string server_id = 1; // Owning server, empty for global modules
  string module_id = 2; // Module container instead of the server's
  string path = 3; // Absolute container directory, empty means /
}

// Container directory contents, one level deep
message ListContainerFilesResponse {
  string path = 1; // Absolute directory that was listed
  repeated FileInfo files = 2;
}

// Download session
message InitFileDownloadResponse {
  string session_id = 1;
  string filename = 2;
  int64 total_size = 3;
}
